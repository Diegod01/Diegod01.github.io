---
title: "Proyecto Inferencia II: Climbers"
author: "Diego Da Rosa"
format:
  pdf:
    pdf-engine: pdflatex
    keep-tex: true
    include-in-header: 
      text: |
        \usepackage{booktabs}
        \usepackage{longtable}
        \usepackage{threeparttable}
execute:
  echo: false
  warning: false
  message: false
  error: true
editor: visual
---



```{r}
#
#Cargar paquetes
library(loo)
library(purrr)
library(bayesrules)
library(tidyverse)
library(rstan)
library(rstanarm)
library(bayesplot)
library(broom.mixed)
library(janitor)
library(ggplot2)
library(knitr)
library(kableExtra)
library(ggplot2)
library(gridExtra)
library(patchwork)
library(posterior)
library(tidybayes)
library(tibble)
library(knitr)
library(parallel)
```

```{r}
data(climbers_sub)
climbers <- climbers_sub 
climbers
```





### Introducción

- El dataset contiene información de interacciones comerciales cliente–propiedad en el mercado inmobiliario y cuenta con 24 variables y 2076 observaciones.

- Algunas variables refieren a las características sociodemográficas del lead/cliente, mientras que otras describen los atributos del inmueble visitado.

- La variable de interés es la decisión de compra (compra: 1 = compró el inmueble, 0 = no compró), la cual es binaria.

- La compra representa un evento de conversión comercial real, análogo a un cierre de venta.

- En el contexto original, el éxito se daba al alcanzar la cima del pico; en esta reconversión, el éxito se interpreta como que el cliente decidió adquirir la propiedad que visitó, lo cual nos lleva naturalmente a plantear un modelo de clasificación con enlace logístico para estimar probabilidades de conversión.

- Esto nos hace pensar en un modelo logístico (regresión logística) para explicar y/o predecir la compra.


Dado que queremos modelar un suceso de la realidad que es binario (compra o no compra de un inmueble), asumimos que la forma adecuada de representar la incertidumbre de cada lead es mediante una distribución Bernoulli, con probabilidad de compra individual $\pi_i$ (probabilidad de conversión del lead i frente a la propiedad que visitó). Por lo tanto, nuestra variable de respuesta $Y$ se distribye: $$Y_i \mid \pi_i \sim \text{Bern}(\pi_i)$$ con:

$$E(Y_i \mid \pi_i) = \pi_i$$ A su vez, a la hora de armar el modelo, utilizaremos las odds. Recordemos que las odds son una manera distinta de mirar las probabilidades, donde comparamos la probabilidad de exito con opuesto: $\text{odds} = \frac{\pi}{1 - \pi}$

Entones: $$Y_i \mid \boldsymbol{\beta_0},{\beta_1}...{\beta_n} \sim \mathrm{Bern}(\pi_i)  \text{ ,  con } \log\!\left(\frac{\pi_i}{1-\pi_i}\right) = \mathbf{x}_i^\top \boldsymbol{\beta}.$$ Con: $$\beta_{0} \sim N(m_{0}, s_{0}^{2}) \\
\beta_{1} \sim N(m_{1}, s_{1}^{2}) \\
\vdots \\
\\
\beta_{n} \sim N(m_{n}, s_{n}^{2})$$

Podemos despejar las probabilidad $\pi_i$ en un modelo logistico de las siguientes maneras:

$$\frac{\pi_i}{1 - \pi_i} = e^{\beta_0 + \beta_1 X_{i1}} \quad\text{o, despejando}\quad \pi_i = \frac{e^{\beta_0 + \beta_1 X_{i1}}}{1 + e^{\beta_0 + \beta_1 X_{i1}}}$$





En cambio, dado que queremos incorporar jerarquias, debemos hacer ciertos cambios sobre el modelo. Mas precisamente, queremos reflejar que los parametros $\beta_i$ dependen de cada grupo.

$$Y_{ij} \mid \pi_{ij} \sim \mathrm{Bern}(\pi_{ij})$$ $$\text{con} \quad \log\!\left(\frac{\pi_{ij}}{1 - \pi_{ij}}\right) = \beta_{0j} + \sum_{k=1}^{p} \beta_k X_{ijk}$$ $$\beta_{0j} \mid \beta_0, \sigma_0 \overset{ind}{\sim} \mathrm{N}(\beta_0, \sigma_0^2) \text{ representa la variabilidad entre grupos}$$ Las Priors globales son:

$$\beta_0 \sim \mathrm{N}(m_0, s_0^2)$$ y $$\beta_k \sim \mathrm{N}(m_k, s_k^2)\quad \text{para } k=1\dots p$$ y el error es $$\sigma_0 \sim \text{Exp}(\lambda)$$ De la misma manera podemos expresar los interceptos como: $$\beta_{0j} = \beta_0 + b_{0j}$$




$$\text{por lo que} \quad \log\!\left(\frac{\pi_{ij}}{1 - \pi_{ij}}\right)
= (\beta_0 + b_{0j}) + \beta_1 X_{ij1} + \beta_2 X_{ij2} + \dots+ \beta_n X_{ijn}$$

En resumen,

Los interceptos específicos de cada grupo $\beta_{0j}$ representan la tasa base de éxito (en log-odds) para cada grupo j. Esto permite diferenciar cada grupo entre si, donde por ejemplo algunos grupos tienen niveles de éxito más altos que otros.

Estos interceptos $\beta_{0j}$ se modelan como distribuidos Normalmente alrededor de un intercepto global $\beta_{0}$ con desviación estándar $\sigma_{0}$. El parámetro $\beta_{0}$ refleja la tasa de éxito promedio entre todos los grupos. La cantidad $\sigma_{0}$ mide la variabilidad entre grupos, es decir, cuánto difieren unos de otros.

De igual manera que en el resto de modelos, los interceptos $\beta_1$, $\beta_2$ ... siguen describiendo las relaciones globales entre las variables.

### Info de las variables:




```{r}
climbers <- climbers %>%
  rename(
    campaña = expedition_id,
    cliente_id = member_id,
    propiedad_id = peak_id,
    propiedad = peak_name,
    año_interaccion = year,
    ciclo_mercado = season,
    sexo = sex,
    edad = age,
    origen = citizenship,
    rol_decision = expedition_role,
    partner_externo = hired,
    interes_max = highpoint_metres,
    compra = success,
    decision_individual = solo,
    premium = oxygen_used,
    lead_fallo = died,
    motivo_fallo = death_cause,
    tam_fallo = death_height_metres,
    friccion = injured,
    tipo_incidente = injury_type,
    tam_incidente = injury_height_metres,
    asesores = count,
    tam_propiedad = height_metres,
    año_primer_listing = first_ascent_year
  )


head(climbers)
```






```{r}
#| echo: false
#| message: false
#| warning: false
#24
variables <- data.frame(
  Variable = c(
    "campaña", "cliente_id", "propiedad_id", "propiedad",
    "año_interaccion", "ciclo_mercado", "sexo", "edad",
    "origen", "rol_decision", "partner_externo",
    "interes_max", "compra", "decision_individual", "premium",
    "lead_fallo", "motivo_fallo", "tam_fallo",
    "friccion", "tipo_incidente", "tam_incidente",
    "asesores", "tam_propiedad", "año_primer_listing"
  ),

  Tipo_dato = c(
    "character", "factor", "factor", "factor",
    "numeric", "factor", "character", "numeric",
    "factor", "factor", "logical",
    "numeric", "logical", "logical", "logical",
    "logical", "factor", "numeric",
    "logical", "factor", "numeric",
    "numeric", "numeric", "numeric"
  ),

  Descripción = c(
    "Identificador de la campaña o fuente de captación del lead",
    "Identificador único del cliente potencial (lead)",
    "Identificador único de la propiedad consultada o visitada",
    "Nombre o tipo de propiedad (casa, apartamento, etc.)",
    "Año en que ocurrió la interacción cliente–propiedad",
    "Ciclo de demanda del mercado inmobiliario (temporada del mercado)",
    "Sexo del cliente",
    "Edad del cliente",
    "País u origen del lead",
    "Rol del lead en la decisión de compra (titular, inversor, cónyuge, etc.)",
    "Indica si el lead provino de un partner externo (TRUE = sí, FALSE = no)",
    "Nivel máximo de interés mostrado en la visita (proxy comercial en metros observados/recorridos)",
    "Conversión de compra (TRUE = compra concretada, FALSE = no concretada)",
    "Indica si la decisión de compra fue individual (TRUE) o compartida/grupal (FALSE)",
    "Indica si la propiedad es premium o tiene amenities diferenciales (TRUE = sí, FALSE = no)",
    "Indica si el lead falló críticamente en el proceso de conversión (TRUE = sí, FALSE = no)",
    "Motivo del fallo crítico de conversión (solo si lead_fallo = TRUE)",
    "Tamaño de la propiedad al momento del fallo crítico (si aplica)",
    "Indica si hubo fricción o incidente en el proceso de compra (TRUE = sí, FALSE = no)",
    "Tipo de incidente en la conversión (crédito rechazado, documentación, tasación baja, etc.)",
    "Tamaño de la propiedad al momento del incidente (si aplica)",
    "Cantidad de asesores/vendedores que atendieron al lead",
    "Metros cuadrados totales de la propiedad",
    "Año en que la propiedad salió al mercado por primera vez (proxy a antigüedad del listing)"
  )
)

knitr::kable(
  variables,
  caption = "Descripción de Variables del Dataset Inmobiliario (Conversión de Compra)",
  booktabs = TRUE,
  longtable = TRUE
) |>
  kableExtra::kable_styling(
    font_size = 8
  )
``` 






```{r}
#| echo: false
#| message: false
#| warning: false

library(dplyr)
library(tidyr)
climbers_sub

# variables numéricas
numeric_vars <- climbers %>% select(where(is.numeric))

resumen_num <- numeric_vars %>%
  summarise(across(
    everything(),
    list(
      Min = ~min(.x, na.rm = TRUE),
      Q1 = ~quantile(.x, 0.25, na.rm = TRUE),
      Median = ~median(.x, na.rm = TRUE),
      Mean = ~mean(.x, na.rm = TRUE),
      Q3 = ~quantile(.x, 0.75, na.rm = TRUE),
      Max = ~max(.x, na.rm = TRUE)
    )
  )) %>%
  pivot_longer(
    everything(),
    names_to = c("Variable", "Estadístico"),
    values_to = "Valor",
    names_pattern = "(.*)_(Min|Q1|Median|Mean|Q3|Max)"
  ) %>%
  pivot_wider(names_from = Estadístico, values_from = Valor)

# variables lógicas
logical_vars <- climbers %>% select(where(is.logical))

resumen_logical <- logical_vars %>%
  summarise(across(everything(), ~sum(.x, na.rm = TRUE))) %>%
  pivot_longer(everything(), names_to = "Variable", values_to = "TRUEs") %>%
  mutate(
    Total = nrow(climbers),
    Porcentaje_TRUE = round(TRUEs / Total * 100, 2)
  )

  


head(climbers)

```



```{r}
#| echo: false
#| message: false
#| warning: false

kable(resumen_num) %>% kable_styling(font_size = 12)
kable(resumen_logical) %>% kable_styling(font_size = 12)
```  





Matriz de Correlacion:

```{r}
climbers <- climbers %>% 
  select(
    -propiedad_id,      # ID original del dataset, no aporta a negocio
    -motivo_fallo,      # detalle de fallo crítico (si no lo vas a usar)
    -tam_fallo,         # tamaño en el momento del fallo (si no lo usas)
    -tipo_incidente,    # tipo de incidente (si no lo vas a modelar)
    -tam_incidente      # tamaño en el momento del incidente (si no lo usas)
  )




library(corrplot)
climbers_num <- climbers[sapply(climbers, is.numeric)]
M <- cor(climbers_num, use = "pairwise.complete.obs")
corrplot(M, method = "color", addCoef.col = "black")


head(climbers)
```


Nueva Variable


Mide cuántos años pasaron desde que la propiedad fue listada por primera vez hasta la interacción actual con el cliente.
Refleja el tiempo de exposición del activo inmobiliario, lo cual puede influir en la probabilidad de compra:

Propiedades con mucha exposición pueden indicar sobreprecio, baja demanda o problemas no observados.

Propiedades nuevas en el mercado suelen generar más interés y conversiones rápidas.

También puede asociarse a estrategias de negocio como descuentos por tiempo de publicación, rotación de inventario y urgencia comercial.

```{r}
climbers <- climbers %>% 
  mutate(antiguedad_años = año_interaccion - año_primer_listing)
```



```{r}
climbers <- climbers %>%
  mutate(
    antiguedad_años = año_interaccion - año_primer_listing,
    tramo_antiguedad = cut(
      antiguedad_años,
      breaks = seq(0, 150, by = 10),
      include.lowest = TRUE
    )
  )
#head(climbers)



rate_tramo <- climbers %>% 
  filter(!is.na(tramo_antiguedad), !is.na(compra)) %>% 
  group_by(tramo_antiguedad) %>% 
  summarise(
    tasa_compra = mean(compra),
    n = n()
  )

ggplot(climbers, aes(x = antiguedad_años, fill = factor(compra))) +
  geom_histogram(binwidth = 5, position = "fill") +
  labs(
    x = "Antigüedad de la propiedad al momento de la interacción (años)",
    y = "Proporción de leads",
    fill = "Compra",
    title = "Proporción de compra por tramo de antigüedad del listing"
  ) +
  scale_x_continuous(limits = c(0, 70), breaks = seq(0, 70, 10)) +
  theme_minimal()

```




```{r}
library(dplyr)
library(ggplot2)
library(patchwork)
library(rlang)
library(dplyr)
library(ggplot2)
library(patchwork)


# 1. Distribución de sexo (barras)
p1 <- climbers %>%
  count(sexo) %>%
  ggplot(aes(x = sexo, y = n, fill = sexo)) +
  geom_col() +
  theme_minimal() +
  ggtitle("Distribución por sexo del lead")

# 2. Distribución por tipo de propiedad (barras)
p2 <-  climbers %>%
  count(propiedad) %>%
  ggplot(aes(x = propiedad, y = n, fill = propiedad)) +
  geom_col() +
  theme_minimal() +
  theme(
    axis.text.x = element_text(size = 6, angle = 45, hjust = 1),
    legend.position = "none"
  ) +
  ggtitle("Distribución por tipo de propiedad")



# 3. Antigüedad del listing al interactuar (histograma)
p3 <- climbers %>%
  mutate(antiguedad = año_interaccion - año_primer_listing) %>%
  ggplot(aes(x = antiguedad)) +
  geom_histogram(
    binwidth = 5,
    fill = "#2C7FB8",
    color = "white",
    alpha = 0.85
  ) +
  theme_minimal() +
  ggtitle("Antigüedad del listing (años)")

# 4. Edad del lead (histograma)
p4 <- climbers %>%
  ggplot(aes(x = edad)) +
  geom_histogram(
    binwidth = 5,
    fill = "#F03B20",
    color = "white",
    alpha = 0.85
  ) +
  theme_minimal() +
  ggtitle("Distribución de edad del lead")

# Panel de 4 gráficos
(p1 | p2) / (p3 | p4)

```


```{r}


library(dplyr)
library(ggplot2)
library(patchwork)

# 1. Ciclo de mercado (estacionalidad de la demanda)
p5 <- climbers %>%
  count(ciclo_mercado) %>%
  ggplot(aes(x = ciclo_mercado, y = n, fill = ciclo_mercado)) +
  geom_col() +
  theme_minimal() +
  theme(axis.text.x = element_text(size = 7, angle = 30, hjust = 1)) +
  ggtitle("Distribución por ciclo de mercado")


# 2. Rol en la decisión de compra
p6 <- climbers %>%
  count(rol_decision) %>%
  ggplot(aes(x = rol_decision, y = n, fill = rol_decision)) +
  geom_col() +
  theme_minimal() +
  theme(
    axis.text.x = element_text(size = 6, angle = 45, hjust = 1),
    legend.position = "none"
  ) +
  ggtitle("Distribución por rol en la decisión")


# 3. Conversión de compra (target binario)
p7 <- climbers %>%
  count(compra) %>%
  mutate(tasa = n/sum(n)) %>%
  ggplot(aes(x = factor(compra), y = tasa, fill = factor(compra))) +
  geom_col() +
  labs(x = "Compra (0=No, 1=Sí)", y = "Proporción") +
  theme_minimal() +
  ggtitle("Tasa de conversión de compra")

# 4. Propiedades premium vs estándar
p8 <- climbers %>%
  count(premium) %>%
  mutate(tasa = n/sum(n)) %>%
  ggplot(aes(x = factor(premium), y = tasa, fill = factor(premium))) +
  geom_col() +
  labs(x = "Premium (0=No, 1=Sí)", y = "Proporción") +
  theme_minimal() +
  ggtitle("Proporción de propiedades premium")


# Panel de 4 gráficos
(p5 | p6) / (p7 | p8)

```





```{r}
library(bayesrules)
library(tidyverse)
library(patchwork)

# ---- A. Edad del cliente ----
p_age <- climbers %>% 
  ggplot(aes(x = factor(compra), y = edad, fill = factor(compra))) +
  geom_boxplot(alpha = 0.7) + 
  labs(title = "A. Distribución de Edad",
       x = NULL, y = "Edad (años)", fill = "Compra") +
  theme_minimal() +
  theme(legend.position = "none")

# ---- B. Máximo interés registrado ----
p_height <- climbers %>%
  ggplot(aes(x = factor(compra), y = interes_max, fill = factor(compra))) +
  geom_boxplot(alpha = 0.7) +
  labs(title = "B. Máximo interés del cliente",
       x = NULL, y = "Interés máximo (m2 o similar)", fill = "Compra") +
  theme_minimal() +
  theme(legend.position = "none")

# ---- C. Tamaño de la propiedad ----
p_peak <- climbers %>%
  ggplot(aes(x = factor(compra), y = tam_propiedad, fill = factor(compra))) +
  geom_boxplot(alpha = 0.7) +
  labs(title = "C. Tamaño de la Propiedad",
       x = NULL, y = "Metros cuadrados", fill = "Compra") +
  theme_minimal() +
  theme(legend.position = "none")

# ---- D. Año de interacción / lead ----
p_year <- climbers %>%
  ggplot(aes(x = factor(compra), y = año_interaccion, fill = factor(compra))) +
  geom_boxplot(alpha = 0.7) +
  labs(title = "D. Año de la Interacción",
       x = NULL, y = "Año", fill = "Compra") +
  theme_minimal() +
  theme(legend.position = "none")

# ---- Panel final ----
panel_numerico <- 
  (p_age + p_height) /
  (p_peak + p_year) +
  plot_annotation(
    title = "Análisis de Variables Numéricas (Contexto Inmobiliario)",
    caption = "Las cajas representan el rango intercuartílico (IQR)"
  )

panel_numerico

```





```{r}
library(bayesrules)
library(tidyverse)
library(patchwork)
library(scales)

# Tema base para barras
tema_barras <- theme_minimal() +
  theme(
    legend.position = "bottom",
    axis.text.x = element_text(angle = 30, hjust = 1)
  )

# ---- A. Propiedad premium vs compra ----
p_oxy <- climbers %>%
  filter(!is.na(premium), !is.na(compra)) %>%
  ggplot(aes(x = as.factor(premium), fill = factor(compra))) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = percent) +
  labs(title = "A. Propiedades Premium", x = "¿Propiedad premium?", y = "Proporción", fill = "Compra") +
  tema_barras

# ---- B. Sexo vs compra ----
p_sex <- climbers %>%
  filter(!is.na(sexo), !is.na(compra)) %>%
  ggplot(aes(x = sexo, fill = factor(compra))) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = percent) +
  labs(title = "B. Sexo del Cliente", x = "Sexo", y = NULL, fill = "Compra") +
  tema_barras

# ---- C. Ciclo de mercado vs compra ----
p_season <- climbers %>%
  filter(!is.na(ciclo_mercado), !is.na(compra)) %>%
  ggplot(aes(x = ciclo_mercado, fill = factor(compra))) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = percent) +
  labs(title = "C. Ciclo de Mercado", x = "Temporada de demanda", y = "Proporción", fill = "Compra") +
  tema_barras

# ---- D. Fricción/incidente vs compra ----
p_injured <- climbers %>%
  filter(!is.na(friccion), !is.na(compra)) %>%
  ggplot(aes(x = as.factor(friccion), fill = factor(compra))) +
  geom_bar(position = "fill") +
  scale_y_continuous(labels = percent) +
  labs(title = "D. Fricción en la Compra", x = "¿Hubo incidentes?", y = NULL, fill = "Compra") +
  tema_barras

# ---- Panel final ----
panel_categorico <- (p_oxy + p_season) / (p_sex + p_injured) +
  plot_layout(guides = "collect") +
  plot_annotation(
    title = "Análisis de Variables Categóricas (Inmobiliario)",
    subtitle = "Probabilidad de compra según factores clave (proporciones)",
    caption = "Gráficos normalizados al 100%"
  ) &
  theme(legend.position = "bottom")

panel_categorico

```





gráfico barras apiladas por proporción. sexo y éxito. se observa que la proporción de éxito de hombre es apenas mayor, sin embargo, al tener tan pocas observaciones sobre mujeres escaladoras, se considera que no hay una relación fuerte como para definir sexo como predictora.

# Definicion de los modelos

USAMOS MODELO DE REGRESION LOGISTICA: nuestra variable de respuesta "Y" es binaria. En este caso es 1 si hubo succes (si la persona llego a la cima de la montana), y 0 si hubo fracaso. Este tipo de regresión es no lineal

$$\begin{aligned}
Y_i & \sim \text{Bern}(\pi_i) \\
\log\left(\frac{\pi_i}{1-\pi_i}\right) & = \beta_0 + \beta_1 X_1 + \beta_2 X_2
\end{aligned}$$

$Y_i \sim \text{Bern}(\pi_i)$ (aleatoria): tener succes con probabilidad de éxito $\pi$.

$\log(\frac{\pi}{1-\pi})$: es la transformación "log odds" que hacemos para poder expresar nuestras probabilidades. nuestro modelo de regresión lineal original nos da resultados en todo el mundo de los reales. sin embargo, cuando trabajamos con variables binarias, nuestro espacio de soluciones pasa a ser entre 0 y 1. tenemos que ajustar esto para poder trabajar nuevamente con los reales, por eso aplicamos la transformación. primero pasamos de "probabilidad" (pi) a "odds" o "chances", haciendo pi/(1-pi). lo que queremos calcular es cuantas veces mas probable es ganar que perder. por ejemplo, si pi=0.5 entonces odds va a ser 1, lo que quiere decir que con probabilidad 50%, la chance de ganar es una vez mas probable que perder. finalmente, le aplicamos logaritmo a las odds, para que nuestro dominio sea todos los reales. por ejemplo, en el caso de oods=1, al aplicar logaritmo obtenemos 0, pero si nuestras odds fueran muy altas, el logaritmo seria un numero positivo grande, y si las odds fueran muy bajas, el logaritmo devuelve un numero negativo.

$\beta_0$ (intercepto): probabilidad de éxito cuando todas las variables $X$ valen 0. "probabilidad base".

$\beta_1$ (pendiente): cuanto aumenta (o disminuye) la probabilidad de éxito si aumentamos $X_1$ en una unidad. Si $\beta > 0$: Aumenta la probabilidad de exito. Si $\beta < 0$: Disminuye la probabilidad.

MODELO DE REGRESION LOGISTICA JERARQUICO

POOLING: si los grupos comparten info entre si

ahora los datos tienen "estructura". Los escaladores pertenecen a grupos (por ejemplo expedicion o monatana).

complete pooling: Ignora la montana. Asume que todas son iguales. (se comparte toda la info)

No Pooling: Trata cada montana de forma independiente. (no se comparte nada de la info)

Jerárquico (Partial Pooling): cada montana es unica ($\beta_{0j}$), pero que todas pertenecen a una "familia de montanas". Las montanas con pocos datos "toman datos" del promedio global. (se comparte algo de info)

$$\begin{aligned}
\text{Nivel 1 (Datos):} \quad \log\left(\frac{\pi_{ij}}{1-\pi_{ij}}\right) & = \beta_{0j} + \beta_1 X_{ij1} \\
\text{Nivel 2 (Grupos):} \quad \beta_{0j} & \sim N(\beta_0, \sigma^2)
\end{aligned}$$

$\beta_{0j}$ (efecto aleatorio/intercepto variando): Cada grupo $j$ (ej. cada montana o expedicion) tiene su propio "punto de partida". $\beta_0$ (Efecto fijo/Hiperparametro de media): Es el promedio global de todas las montanas. $\sigma$ (desviacion del grupo): Mide que tan diferentes son las montanas entre si. Si $\sigma$ es bajo: Todas las montanas se parecen mucho (alto pooling). Si $\sigma$ es alto: Las montanas son muy distintas entre si (pooling bajo).

OBS: DEFINIMOS PRIORIS POCO INFORMATIVAS por que? Normal (0,2.5): queremos evitar hacer overfitting cuando tenemos pocos datos (en nuestro caso "limpiamos" mucho nuestra base de datos original) en la escala logística, coeficientes mayores a 5 implican certeza prácticamente. nosotros nos queremos posicionar "en el medio" y dejar que los datos nos digan que paso. por otro lado, centramos en cero ya que queremos tener una postura neutral. asumimos a priori que las variables no tienen efecto sobre el éxito (por eso esta es 0). nuevamente dejamos que los datos nos muestren que pasa. exp(1): varianza no puede ser negativa, solo trabajamos con valores positivos.

## Modelo 1:

Definiremos el modelo 1 como el modelo con complete pooling con los siguientes predictores: oxigeno, season, y year_since_first_ascent. Es decir, asumimos que todos los escaladores distribuyen de igual manera.

OBS: no incluimos peak ya que no nos importa en que montaña estamos, tratamos a todas por igual. Si incluimos peak estamos diferenciando a las montañas. ("ignoramos los grupos y metemos a todos en la misma bolsa").

OBS: este modelo es nuestro punto de partida

De esta manera:

$$\boxed{
\hspace{0.5em}
\begin{aligned}
Y_{i} | \beta_{0}, \beta_1, \beta_2, \beta_3 & \sim \text{Bern}(\pi_{i}) && \text{(verosimilitud)} \\
\text{donde} \quad \log\left(\frac{\pi_{i}}{1-\pi_{i}}\right) & = \beta_{0} + \beta_1 X_{i1} + \beta_2 X_{i2} + \beta_3 X_{i3} \\
\beta_{0} & \sim N(0, 2.5^2) && \text{(intercepto global)} \\
\beta_1, \beta_2, \beta_3 & \overset{ind}{\sim} N(0, 2.5^2) && \text{(coeficientes fijos)}
\end{aligned}
\hspace{0.5em}
}$$

beta1, beta2 y beta 3 son nuestras predictoras (fijas)

variables:

$Y_i$: Éxito del escalador $i$. $X_{i1}$: Uso de oxígeno. $X_{i2}$: Estación del año. $X_{i3}$: Años desde el primer ascenso.

## Modelo 2:

Partial pooling por **expedicion**. Agregamos jerarquía en la expedición j, tal como el libro.

$$
\boxed{
\hspace{0.5em}
\begin{aligned}
Y_{ij} | \beta_{0j}, \beta_1, \beta_2, \beta_3 & \sim \text{Bern}(\pi_{ij}) && \text{(verosimilitud)} \\
\text{with} \quad \log\left(\frac{\pi_{ij}}{1-\pi_{ij}}\right) & = \beta_{0j} + \beta_1 X_{ij1} + \beta_2 X_{ij2} + \beta_3 X_{ij3}\\
\beta_{0j} | \beta_{0c}, \sigma_{exp} & \overset{ind}{\sim} N(\beta_{0c}, \sigma_{exp}^2) && \text{(variabilidad entre expediciones)} \\
\beta_{0c} & \sim N(0, 2.5^2) && \text{(intercepto global)} \\
\beta_1, \beta_2, \beta_3 & \overset{ind}{\sim} N(0, 2.5^2) && \text{(coeficientes fijos)} \\
\sigma_{exp} & \sim \text{Exp}(1) && \text{(desvío est. entre expediciones)}
\end{aligned}
\hspace{0.5em}
}
$$





beta0, beta1 ,beta2 y beta3 son nuestras predictoras (fijas)

variables:

$Y_i,j$: Éxito del escalador i en la expedición j. $X_{i,j,1}$: Uso de oxígeno. $X_{i,j,2}$: Estación del año. $X_{i,j,3}$: Años desde el primer ascenso.

## Modelo 3

Partial Pooling por **peak** (agregamos jerarquía pico en vez de escalación, para mostrar lo que a priori es un mal modelo o mal conceptualmente hablando agrupar por pico ). Hay individuos dentro del mismo grupo muy divergentes entre si.

$$\boxed{
\hspace{0.5em}
\begin{aligned}
Y_{ik} | \beta_{0k}, \beta_1, \beta_2, \beta_3 & \sim \text{Bern}(\pi_{ik}) && \text{(verosimilitud)} \\
\text{with} \quad \log\left(\frac{\pi_{ik}}{1-\pi_{ik}}\right) & = \beta_{0k} + \beta_1 X_{ik1} + \beta_2 X_{ik2} + \beta_3 X_{ik3} \\
\beta_{0k} | \beta_{0c}, \sigma_{peak} & \overset{ind}{\sim} N(\beta_{0c}, \sigma_{peak}^2) && \text{(variabilidad entre picos)} \\
\beta_{0c} & \sim N(0, 2.5^2) && \text{(intercepto global)} \\
\beta_1, \beta_2, \beta_3 & \overset{ind}{\sim} N(0, 2.5^2) && \text{(coeficientes fijos)} \\
\sigma_{peak} & \sim \text{Exp}(1) && \text{(desvío est. entre picos)}
\end{aligned}
\hspace{0.5em}
}$$













beta1, beta2 y beta3 son nuestras predictoras

variables

$Y_i,k$: Éxito del escalador i en peak k. $X_{i,k,1}$: Uso de oxígeno. $X_{i,k,2}$: Estación del año. $X_{i,k,3}$: Años desde el primer ascenso.

### Modelo 4

Jerarquía con Season.

$$\boxed{
\hspace{0.5em}
\begin{aligned}
Y_{is} | \beta_{0s}, \beta_1, \beta_2, \beta_3 & \sim \text{Bern}(\pi_{is}) && \text{(verosimilitud)} \\
\text{with} \quad \log\left(\frac{\pi_{is}}{1-\pi_{is}}\right) & = \beta_{0s} + \beta_1 X_{is1} + \beta_2 X_{is2} + \beta_3 X_{is3} \\
\beta_{0s} | \beta_{0c}, \sigma_{peak} & \overset{ind}{\sim} N(\beta_{0c}, \sigma_{peak}^2) && \text{(variabilidad entre picos)} \\
\beta_{0c} & \sim N(0, 2.5^2) && \text{(intercepto global)} \\
\beta_1, \beta_2, \beta_3 & \overset{ind}{\sim} N(0, 2.5^2) && \text{(coeficientes fijos)} \\
\sigma_{peak} & \sim \text{Exp}(1) && \text{(desvío est. entre picos)}
\end{aligned}
\hspace{0.5em}
}$$






beta1, beta2 y beta3 son nuestras predictoras

variables

$Y_i,s$: Éxito del escalador i en season s. $X_{i,s,1}$: Uso de oxígeno. $X_{i,s,2}$: Estación del año. $X_{i,s,3}$: Años desde el primer ascenso.

```{r}
# Define success rate function para los pp check 
success_rate <- function(x){mean(x == 1)}


head(climbers)

```

# Corrida de los modelos

```{r}
# climbers <- climbers %>% 
#   mutate(
#     antiguedad_años = año_interaccion - año_primer_listing
#   )
# 
# 
# head(climbers)
```



## Modelos

# Modelo 1
```{r}
options(mc.cores = parallel::detectCores())

# modsj <- stan_glm(
#   compra ~ premium + ciclo_mercado + antiguedad_años,
#   data = climbers,
#   family = binomial,
#   prior_intercept = normal(0, 2.5, autoscale = TRUE),
#   prior = normal(0, 2.5, autoscale = TRUE),
#   prior_aux = exponential(1, autoscale = TRUE),
#   chains = 4, iter = 10000, seed = 84735
# )



# 
# saveRDS(modsj, "modsj.rds")

modsj <- readRDS("modsj.rds")

ppcheck1 <- pp_check(modsj, nreps = 100, plotfun = "stat", stat = "success_rate") +
  xlab("Tasa de compra")
ppcheck1

```



La barra vertical es el éxito promedio del dataframe. El resto en celeste es la tasa de éxito en nuestros 100 dataframes simulados.


Modelo 2

```{r}
options(mc.cores = parallel::detectCores())

# modje <- stan_glmer(
#   compra ~ ciclo_mercado + antiguedad_años + premium + (1 | campaña),
#   data = climbers,
#   family = binomial,
#   prior_intercept = normal(0, 2.5, autoscale = TRUE),
#   prior = normal(0, 2.5, autoscale = TRUE),
#   prior_covariance = decov(reg = 1, conc = 1, shape = 1, scale = 1),
#   chains = 4, iter = 10000, seed = 84735
# )




 
# saveRDS(modje, "modje.rds")

modje <- readRDS("modje.rds")

ppcheck2 <- pp_check(modje, nreps = 100, plotfun = "stat", stat = "mean") +
  xlab("Tasa de compra")
ppcheck2


```





Notamos que este modelo tiene una mejoría respecto al anterior, pero de igual forma pensamos que todavía se puede mejorar más



Modelo 3

```{r}
options(mc.cores = parallel::detectCores())

# modjp <- stan_glmer(
#   compra ~ ciclo_mercado + antiguedad_años + premium + (1 | propiedad),
#   data = climbers,
#   family = binomial,
#   prior_intercept = normal(0, 2.5, autoscale = TRUE),
#   prior = normal(0, 2.5, autoscale = TRUE),
#   prior_covariance = decov(reg = 1, conc = 1, shape = 1, scale = 1, scale_cov = 1),
#   chains = 4, iter = 10000, seed = 84735
# )

# saveRDS(modjp, "modjp.rds")

modjp <- readRDS("modjp.rds")

ppcheck3 <- pp_check(modjp, nreps = 100, plotfun = "stat", stat = "success_rate") +
  xlab("Tasa de compra")
ppcheck3

```















# Modelo 4

```{r}
library(dplyr)
library(ggplot2)
library(rstanarm)
library(bayesplot)

options(mc.cores = parallel::detectCores())


# ---- Modelo multinivel (efecto aleatorio por ciclo de mercado) ----

# modjs <- stan_glmer(
#   compra ~ ciclo_mercado + antiguedad_años + premium + (1 | ciclo_mercado),
#   data = climbers,
#   family = binomial,
#   prior_intercept = normal(0, 2.5, autoscale = TRUE),
#   prior = normal(0, 2.5, autoscale = TRUE),
#   prior_covariance = decov(reg = 1, conc = 1, shape = 1, scale = 1),
#   chains = 4, iter = 10000, seed = 84735
# )



# saveRDS(modjs, "modjs.rds")


#Cargar modelo
modjs <- readRDS("modjs.rds")

# Posterior predictive check adaptado
ppcheck4 <- pp_check(modjs, nreps = 100, plotfun = "stat", stat = "success_rate") +
  xlab("Tasa de compra")
ppcheck4

# Diagnóstico MCMC
mcmc_trace(modjs)
```










# PP_Checks:

```{r}
p1 <- ppcheck1 +
  labs(title = "Modelo 1", subtitle = "Sin jerarquía (Complete Pooling)") +
  theme(legend.position = "none")

p2 <- ppcheck2 +
  labs(title = "Modelo 2", subtitle = "Jerarquía por campaña") +
  theme(legend.position = "none")

p3 <- ppcheck3 +
  labs(title = "Modelo 3", subtitle = "Jerarquía por propiedad") +
  theme(legend.position = "none")

p4 <- ppcheck4 +
  labs(title = "Modelo 4", subtitle = "Jerarquía por ciclo de mercado") +
  theme(legend.position = "none")

panel_ppcheck <- (p1 + p2) / (p3 + p4) +
  plot_annotation(
    title = "pp_check para los cuatro modelos (Inmobiliario)"
  )


panel_ppcheck
```





Se observa de forma visual que no existen diferencias sustanciales entre los modelos. Se podría decir que en principio no aporta una mejoría realizar agrupamiento.

# MCMC diagnostics


```{r}
#| results: 'hide'
#| echo: false

# Deja los gráficos
mcmc_trace(modjs, size = 0.1)
mcmc_dens_overlay(modjs)
mcmc_acf(modjs)


# CORRECCIÓN AQUÍ:
resumen_stats <- as.data.frame(summary(modjs))
# Quitamos paréntesis de los nombres de las filas para que LaTeX no explote
rownames(resumen_stats) <- gsub("[()]", "", rownames(resumen_stats))




resumen_stats %>% 
  head(10) %>% 
  kable(caption = "Resumen estadístico del modelo") %>%
  kable_styling(latex_options = "hold_position")


```




```{r}
# loo_modsj <- loo(modsj)
# loo_modje <- loo(modje)
# loo_modjp <- loo(modjp)
# loo_modjs <- loo(modjs)

#comparacion
#loo_compare(loo_modsj, loo_modje, loo_modjp, loo_modjs)
```







```{r}
library(tidyverse)

resumen_post <- tidy(modje, effects = "fixed", conf.int = TRUE, conf.level = 0.95)

# IMPORTANTE: No escribas 'resumen_post' solo en una línea, 
# eso lo imprime en crudo y rompe el PDF.

resumen_post %>%
  mutate(term = case_when(
    term == "(Intercept)" ~ "Intercepto",
    term == "seasonAutumn" ~ "Ciclo_Mercado_Autumn",
    term == "seasonWinter" ~ "Ciclo_Mercado_Winter",
    term == "seasonSummer" ~ "Ciclo_Mercado_Summer",
    term == "year_since_first_ascent" ~ "Antiguedad_Años",
    term == "oxygen_usedTRUE" ~ "Premium_TRUE",
    TRUE ~ term
  )) %>%
  kable(digits = 3, format = "latex", booktabs = TRUE, 
        caption = "Estimación de efectos fijos") %>%
  kable_styling(latex_options = c("striped", "hold_position"))



```






```{r}
# 1. Creamos el data frame con los nombres exactos que pide el modelo
newdata_alquiler <- data.frame(
  oxygen_used = c(FALSE, TRUE),     # Lo que tú llamas 'premium'
  season = "Spring",        # Lo que tú llamas 'ciclo_mercado'
  year_since_first_ascent = 0,     # Lo que tú llamas 'antiguedad_años'
  expedition_id = "NuevaCampaña"   # Lo que tú llamas 'campaña'
) 

# 2. Predicción de probabilidades (esto ya no debería dar error)
post_preds_alquiler <- posterior_epred(
  modje, 
  newdata = newdata_alquiler, 
  allow.new.levels = TRUE
)

# 3. Organizamos los resultados con tus etiquetas de negocio para el gráfico
df_plot_alquiler <- data.frame(
  Estandar = post_preds_alquiler[,1],
  Premium  = post_preds_alquiler[,2]
) %>% 
  pivot_longer(
    cols = everything(), 
    names_to = "Servicio", 
    values_to = "Probabilidad"
  )

# 4. Gráfica adaptada al enfoque inmobiliario
ggplot(df_plot_alquiler, aes(x = Probabilidad, fill = Servicio)) +
  geom_density(alpha = 0.7) +
  scale_fill_manual(values = c("Estandar" = "#E74C3C", "Premium" = "#2ECC71")) +
  labs(
    title = "Efecto del Servicio Premium en la Probabilidad de Compra",
    subtitle = "Simulación basada en el modelo jerárquico (modje)",
    x = "Probabilidad de éxito (Compra)",
    y = "Densidad",
    fill = "Tipo de Servicio"
  ) +
  theme_minimal() +
  theme(legend.position = "top")


```




```{r}
# 1. Extraemos los efectos aleatorios (los interceptos de cada campaña)
# Nota: Seguimos usando 'expedition_id' porque así está guardado en el modelo modje
random_effects <- ranef(modje)$expedition_id 
colnames(random_effects) <- "Intercepto_Desvio"

# 2. Seleccionamos una muestra de 30 campañas al azar para visualizar la variabilidad
set.seed(123)
sample_campañas <- random_effects %>%
  as.data.frame() %>%
  rownames_to_column("Campaña_ID") %>%
  sample_n(30) %>%
  arrange(Intercepto_Desvio) # Ordenado de menor a mayor éxito relativo

# 3. Caterpillar Plot (Gráfico de oruga) adaptado al enfoque inmobiliario
ggplot(sample_campañas, aes(x = Intercepto_Desvio, y = reorder(Campaña_ID, Intercepto_Desvio))) +
  geom_point(size = 3, color = "#6584db") +
  # Línea roja representa el promedio global del mercado
  geom_vline(xintercept = 0, linetype = "dashed", color = "red") + 
  labs(
    title = "Variabilidad del éxito entre Campañas (Muestra aleatoria de 30)",
    subtitle = "Puntos a la derecha de la línea roja indican campañas con éxito superior al promedio",
    x = "Diferencia respecto al promedio global (Log-Odds)",
    y = "Identificador de Campaña"
  ) +
  theme_minimal() +
  theme(
    axis.text.y = element_text(size = 7),
    plot.title = element_text(face = "bold")
  )

```




```{r}
library(modelr)
library(tidybayes)

# 1. Crear el grid de datos
# Usamos los nombres que el modelo espera para que 'add_fitted_draws' funcione
grid_inmobiliario <- climbers %>%
  data_grid(
    # Eje x: Usamos el nombre original 'year_since_first_ascent' (que es tu 'antiguedad_años')
    year_since_first_ascent = seq_range(antiguedad_años, n = 100),
    
    # Comparación de servicio Premium (originalmente 'oxygen_used')
    oxygen_used = c(TRUE, FALSE), 
    
    # Ciclo de mercado base (originalmente 'season')
    season = "Spring",
    
    # Referencia de campaña (originalmente 'expedition_id')
    expedition_id = "Campaña_Promedio" 
  )


# 2. Generar las líneas predictivas (draws)
# re_formula = NA ignora la variabilidad de campañas específicas para ver la tendencia general
plot_lines <- grid_inmobiliario %>%
  add_fitted_draws(modje, n = 50, re_formula = NA) 

# 3. Gráfica adaptada
ggplot(plot_lines, aes(x = year_since_first_ascent, y = .value, color = oxygen_used)) +
  # Líneas de incertidumbre
  geom_line(aes(group = paste(oxygen_used, .draw)), alpha = 0.15) +
  
  # Línea de tendencia promedio
  stat_summary(fun = mean, geom = "line", aes(group = oxygen_used), size = 1.2) +
  
  # Colores y etiquetas de negocio
  scale_color_manual(values = c("FALSE" = "#E74C3C", "TRUE" = "#2ECC71"),
                     labels = c("Estándar", "Premium")) +
  
  labs(
    title = "Probabilidad de éxito según Antigüedad y Tipo de Servicio",
    x = "Antigüedad (Años desde el primer registro)",
    y = "Probabilidad de Compra",
    color = "Tipo de Servicio"
  ) +
  theme_minimal() +
  theme(legend.position = "top")
```


Modelo sin oxigeno 

```{r}
# Modelo sin oxigeno 
modje_sin_oxi <- readRDS("modje_sin_oxi.rds")

# CORRECCIÓN AQUÍ:
tidy(modje_sin_oxi , effects = "fixed", conf.int = TRUE, conf.level = 0.80) %>%
  mutate(term = gsub("[()]", "", term)) %>% # Limpieza de seguridad
  kable(caption = "Estimación de efectos fijos para modelo sin oxígeno") %>%
  kable_styling(latex_options = "hold_position")
```




```{r}
# Limpieza de datos usando los nombres de las variables tras el rename
climbers_common <- climbers %>%
  drop_na(
    compra,               # Antes: success
    ciclo_mercado,        # Antes: season
    premium,              # Antes: oxygen_used
    campaña,              # Antes: expedition_id
    antiguedad_años       # Antes: year_since_first_ascent
  )
```


```{r}
# Modelo sin premium

modje_sin_premium <- stan_glmer(
  compra ~ ciclo_mercado + antiguedad_años + (1 | campaña),
)
  

# Modelo sin años

modje_sin_anios <- stan_glmer(
  compra ~ season + oxygen_used + (1 | expedition_id), 
)

  
  




# Tabla  Modelo sin premium
library(knitr)

tabla <- matrix(
  c(0.0, 0.0,
    -167.3, 19.1),
  nrow = 2,
  byrow = TRUE
)

colnames(tabla) <- c("elpd_diff", "se_diff")
rownames(tabla) <- c("modje", "modje_sin_premium")

kable(tabla)




# Tabla Modelo sin años

library(knitr)

tabla2 <- matrix(
  c(0.0, 0.0,
    -0.8, 0.4),
  nrow = 2,
  byrow = TRUE
)

colnames(tabla2) <- c("elpd_diff", "se_diff")
rownames(tabla2) <- c("modje", "modje_sin_años")

kable(tabla2)
```











```{r}
# Cargamos y procesamos en el mismo lugar para evitar que se pierda el objeto
modje_sin_anios <- readRDS("modje_sin_anios.rds")
modje_original <- readRDS("modje_original.rds")


library(broom.mixed)
library(dplyr)
library(knitr)
library(kableExtra)

resumen_post_sin <- tidy(modje_sin_anios, effects = "fixed", conf.int = TRUE, conf.level = 0.95)

resumen_post_sin %>%
  mutate(term = gsub("[()]", "", term)) %>% # Elimina paréntesis de forma global
  select(term, estimate, std.error, conf.low, conf.high) %>%
  kable(digits = 3, caption = "Estimación de efectos fijos (Modelo sin años)") %>%
  kable_styling(latex_options = "hold_position")
```


```{r}
#modje_sin_anios <- readRDS("modje_sin_anios.rds")
```



```{r}

# 
# resumen_post <- tidy(modje_sin_anios, effects = "fixed", conf.int = TRUE, conf.level = 0.95)
# # 
# resumen_post %>%
#   select(term, estimate, std.error, conf.low, conf.high) %>%
#   kable(digits = 3, caption = "Estimacion de efectos fijos ") %>%
#   kable_styling()
```




```{r}
# Bloque de carga (Asegúrate de que este bloque se ejecute)
# if (file.exists("modje_sin_anios.rds")) {
#   modje_sin_anios <- readRDS("modje_sin_anios.rds")
# } else {
#   stop("¡El archivo modje_sin_anios.rds no se encuentra en el directorio de trabajo!")
# }
# 
# modje_original <- readRDS("modje_original.rds")

```



```{r}
# 1. Creamos el data frame con los 4 ciclos de mercado
# Usamos 'oxygen_used = TRUE' para simular el escenario "Premium" en cada ciclo
newdata_ciclos <- data.frame(
  season = c("Spring", "Summer", "Autumn", "Winter"), # Ciclo de mercado
  oxygen_used = TRUE,                                # Servicio Premium
  year_since_first_ascent = 0,                       # Antigüedad base
  expedition_id = "NuevaCampaña"                     # ID para efecto aleatorio
)


# 2. Predicción de las probabilidades posteriores
post_preds_ciclos <- posterior_epred(modje, newdata = newdata_ciclos, allow.new.levels = TRUE)

# 3. Organización de datos para graficar
df_ciclos <- data.frame(post_preds_ciclos)
# Asignamos los nombres de los ciclos a las columnas
colnames(df_ciclos) <- newdata_ciclos$season



df_plot_ciclos <- df_ciclos %>%
  pivot_longer(
    cols = everything(), 
    names_to = "season", 
    values_to = "Probabilidad"
  )

# 4. Gráfica adaptada (Boxplot)
ggplot(df_plot_ciclos, aes(x = reorder(season, Probabilidad, FUN = median), y = Probabilidad, fill = season)) +
  # El boxplot muestra la mediana y la incertidumbre de la predicción
  geom_boxplot(width = 0.4, color = "black", alpha = 0.7) +
  # Usamos una paleta de colores profesional
  scale_fill_brewer(palette = "Set3") +
  labs(
    title = "Probabilidad de Éxito según el Ciclo de Mercado",
    subtitle = "Simulación para propiedades con Servicio Premium",
    x = "Ciclo de Mercado (Temporada)",
    y = "Probabilidad de Compra / Alquiler"
  ) +
  theme_minimal() +
  theme(
    legend.position = "none",
    plot.title = element_text(face = "bold")
  )
```


Prediccion

```{r}
# 1. Definir datos para la nueva campaña (sin considerar antigüedad)
new_campaña_data <- data.frame(
  oxygen_used = c(FALSE, TRUE, FALSE, TRUE, FALSE, TRUE, FALSE, TRUE), 
  season = c("Spring", "Spring", "Winter", "Winter", "Autumn", "Autumn", "Summer", "Summer"),
  expedition_id = "Campaña_Futura" 
)

# 2. Predicciones posteriores
set.seed(84735)
# Usamos posterior_predict para obtener los resultados binarios (0 o 1) simulados
pred_binaria <- posterior_predict(
  modje_sin_anios, 
  newdata = new_campaña_data, 
  allow.new.levels = TRUE
)

# 3. Cálculo de probabilidad promedio de éxito
# colMeans sobre una predicción binaria nos da la proporción de éxitos (probabilidad)
prob_exito <- colMeans(pred_binaria)

#4. Tabla con resultados adaptados al negocio
(new_campaña_data)

data.frame(
  Servicio = ifelse(new_campaña_data$oxygen_used, "Premium", "Estándar"),
  Ciclo_Mercado = new_campaña_data$season,
  Probabilidad_Compra = round(prob_exito, 3)
) %>%
  arrange(desc(Probabilidad_Compra)) %>% # Ordenamos para ver lo más exitoso arriba
  kable(caption = "Predicción de éxito para una nueva Campaña Inmobiliaria") %>%
  kable_styling(bootstrap_options = c("striped", "hover", "condensed"))


```







Si la nueva exploración utiliza oxigeno tiene mas chances de tener success

classification summary con cutoff 0.5

```{r}
data(climbers_sub)
climbers <- climbers_sub 
#climbers
```

```{r}
library(knitr)


tabla3 <- data.frame(
  y = c("FALSE", "TRUE"),
  `0` = c(1158, 74),
  `1` = c(111, 733),
  check.names = FALSE
)

kable(tabla3, align = "c")

```








```{r}
set.seed(84735)
classification_summary(model = modje_sin_anios, data = climbers, cutoff = 0.5)
```

classification summary con cutoff 0.8

```{r}
set.seed(84735)

classification_summary(model = modje_sin_anios, data = climbers, cutoff = 0.8)
```

cut off de 0.3

```{r}
set.seed(84735)

classification_summary(model = modje_sin_anios, data = climbers, cutoff = 0.3)
```
